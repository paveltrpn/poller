{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "cryptest",
            "program": "${workspaceFolder}/build/test/io_timer",
            "args": [],
            "cwd": "${workspaceFolder}/build"
        }
    ]
}
